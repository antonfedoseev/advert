version: '3.8'

services:
  producer:
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: /app/server/producer/cmd/producer
    container_name: producer
    ports:
      - "7979:7979"
      - "44001:4001"
    security_opt:
      - "apparmor:unconfined"
      - "seccomp:unconfined"
    cap_add:
      - SYS_PTRACE
    volumes:
      - server-data:/app/server/producer
    networks:
      - pet-backend-producer
      - pet-backend-shared

volumes:
  server-data:
    name: pet-server-vol

networks:
  pet-backend-producer:
    external: true
  pet-backend-shared:
    external: true